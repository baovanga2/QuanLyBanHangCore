@model IEnumerable<Category>

@{
    ViewData["Title"] = "Quản lý loại sản phẩm";
}

@section Styles
{
    @{
        await Html.RenderPartialAsync("ThuVienBangStylespartial.cshtml");
    }
}

<p>
    <a asp-action="Create" class="btn cur-p btn-outline-primary"><i class="fas fa-plus"></i> Thêm mới</a>
</p>

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Danh sách loại sản phẩm</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered table-striped" id="dtLSPvaNSX" width="100%" cellspacing="0">
                <thead class="thead-light">
                    <tr>
                        <th class="w-50">
                            @Html.DisplayNameFor(model => model.Ten)
                        </th>
                        <th class="text-center">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Ten)
                            </td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-primary"><i class="fas fa-tools"></i></a> |
                                <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-primary"><i class="fas fa-list"></i></a> |
                                <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm btn-primary"><i class="fas fa-trash"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts
{
    @{
        await Html.RenderPartialAsync("ThuVienBangScriptsPartial.cshtml");
        await Html.RenderPartialAsync("KichHoatBangScriptsPartial.cshtml");
        await Html.RenderPartialAsync("KichHoatDieuHuongScriptsPartial.cshtml", "LSP");
    }
}